{
  "name": "docker-mcp-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --enable-source-maps dist/server.js || (npm run build && node --enable-source-maps dist/server.js)"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "dockerode": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/dockerode": "^3.3.26",
    "typescript": "^5.6.3"
  }
}
